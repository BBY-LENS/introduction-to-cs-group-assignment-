# Analysis Folder

This is where each group member adds their individual Python scripts for the assignment.  
- Use separate files like `question3.py`, `question4.py`, etc.  
- Each file should load the dataset from `data/` and perform the required analysis.  
- Keep code clean and well‑commented so teammates can understand your part.
